# Requirements for Soft Prompt Adversarial Training (SoftPAT)
#
# Install with: pip install -r requirements.txt
#

# Core dependencies
torch>=2.0.0
transformers>=4.30.0
numpy>=1.21.0
pandas>=1.3.0
tqdm>=4.60.0

# FastChat for conversation templates
fschat>=0.2.0

# Optional: for better CUDA performance
accelerate>=0.20.0
